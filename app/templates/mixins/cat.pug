mixin cat(data)
	.cat
		.title.color-main.mb-15.cat__title= data.title

		a.cat__img-wrap.mb-15(href= data.link)
			.cat__img(style='background-image: url(' + data.img + ');')

		if data.desc
			.cat__desc.mb-15= data.desc

		.cat__btn-wrap
			a.btn.cat__btn(href= data.link) Подробнее